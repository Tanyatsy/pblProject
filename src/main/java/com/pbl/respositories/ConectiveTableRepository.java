package com.pbl.respositories;

import com.pbl.models.ConnectiveTable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConectiveTableRepository extends JpaRepository<ConnectiveTable, Integer> {
}
